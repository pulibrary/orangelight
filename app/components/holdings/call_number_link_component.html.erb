<td>
<% unless cn_value.nil? %>
  <%= holding['call_number'] %>
  <a href="<%= browse_url %>" class="browse-cn"
    data-original-title="<%= original_title %>">
    <span class="link-text" aria-label="Call number browse"><%= I18n.t('blacklight.holdings.browse') %></span>
  </a>
<% end %>
</td>
