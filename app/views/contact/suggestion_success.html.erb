<%= render Blacklight::System::ModalComponent.new do |component| %>
  <% component.title { t('blacklight.suggestion.form.title') } %>

  <%= render partial: '/shared/flash_msg' %>
  <span data-blacklight-modal="close"></span>
<% end %>
