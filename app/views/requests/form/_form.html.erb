<% @page_title = "Request: #{@request.requestable.first.title}" || "Request a title" %>
<% if !@user.guest && @patron.blank? %>
  <%= render partial: 'auth_user_lookup_fail' %>
<% else %>
  <%= render partial: 'request_form' %>
<% end %>