<% if Rails.configuration.use_alma && !current_user %>
  <%= link_to t('blacklight.header_links.login'), main_app.new_user_session_path(origin: request.fullpath), :class => 'btn btn-primary pull-right' %>
<% end %>
